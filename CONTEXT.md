# 应用自迭代

本领域描述用户通过改进应用本身来满足需求的过程。

## Language

**自迭代 Agent**：
专门负责推动本应用自迭代的执行者；需求讨论和问题解释服务于应用改进，不承担通用问答。
_Avoid_：通用助手、问答助手

**自迭代**：
通过改变本应用的能力满足用户需求，并使改进能够验证、持续使用、继续修改和撤回的过程。
_Avoid_：对话回复、临时演示

**候选**：
尚未应用到正式使用环境、供验证和体验的应用变更版本。
_Avoid_：已生效版本

**应用确认**：
用户针对已经生成并验证的具体候选，同意让其成为正式使用版本的决定。
_Avoid_：方案确认、无条件授权

**需求澄清**：
自迭代 Agent 在规划阶段与用户明确具体改进的目标和行为预期的过程，不代表用户已同意应用候选。
_Avoid_：应用确认

**能力缺口**：
满足已明确的改进目标所需、但当前应用尚未提供的能力，不等于用户需求不明确。
_Avoid_：需求歧义

**变更**：
为满足一个明确的改进目标而整体生成、验证和应用的一组修改，可以同时涉及业务行为及其所需的基础能力。
_Avoid_：单个插件、单个文件

**执行计划**：
开始执行前向用户展示的本次改进的大致过程，是查看后续进展的依据。
_Avoid_：执行结果

**业务验收**：
判断应用行为是否满足明确业务要求的标准；用户改变要求时可经确认建立新修订。
_Avoid_：系统保护约束

**系统保护约束**：
保障自迭代过程可控、可信和可恢复，且不能由普通自迭代修改的规则。
_Avoid_：业务验收

**UI 贡献**：
插件向宿主声明、由宿主在白名单区域呈现的可序列化界面贡献；落在宿主控制的渲染契约内，不替换受保护外壳。
_Avoid_：业务区块、自定义页面、插槽组件、任意页面挂载
