// Builtins and generated workflow bundles use the same local type-import seam.
// The candidate compiler supplies its own trusted contract at this path.
export type { Workflow } from "../../shared/contracts.js";
