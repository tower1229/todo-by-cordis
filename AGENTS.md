# 项目协作规则

请用中文沟通，风格直接、务实、基于证据，先给结论。

- 明确要求实现、修复、修改、升级或保存时默认执行；提问、解释、评审、调研或方案比较默认只读。
- 修改前读取适用指令及相关代码、文档、配置和测试，并检查工作区状态；不要凭文件名或通用经验猜测。
- 优先遵循仓库现有架构、工具链、命名和目录；严格限制任务范围，不顺手重构、重命名或清理无关代码。
- 已明确的约束不反复讨论；证据冲突时指出冲突。只有明确要求完整替换时才移除旧实现，仍不得扩大到无关模块。
- 不丢弃、覆盖、暂存或提交用户已有的无关改动。破坏性、不可逆、发布、部署或外部写入操作先确认。
- 使用仓库现有脚本运行与改动匹配的验证；不得声称未运行的检查已通过，并区分已有失败和本次引入的失败。
- Review 默认只读；按严重程度列出可证实的问题，给出文件、行号、影响和修复方向；无问题时说明测试缺口。
- 不因完成修改而自动 commit、push、创建 PR 或部署；明确要求提交时默认创建新 commit，不 amend，不跳过 hooks。
- 不主动新建 Markdown 或 README；明确要求保存方案时创建真实文件。
- TypeScript 不使用 enum；需要运行时值时使用 as const 并推导联合类型。不使用 any，边界例外需限制范围并说明。
- 最终简洁汇报结果、修改文件、实际验证及残余风险。

## Agent skills

### Issue tracker

使用当前项目 GitHub Issues：`tower1229/todo-by-cordis`。见 [配置](docs/agents/issue-tracker.md)。

### Triage labels

使用默认五个分流标签。见 [标签映射](docs/agents/triage-labels.md)。

### Domain docs

采用单领域布局：根目录 `CONTEXT.md` 和 `docs/adr/`。见 [阅读规则](docs/agents/domain.md)。
