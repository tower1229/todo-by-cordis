# 哆啦AI梦 · Design

用户已明确采用 Microsoft To Do 的极简风格：干净、克制，界面自身说明操作。该方向覆盖之前暖白青绿、灵动助手和独立进化区的方案。

场景：白天在桌面或手机上快速记录任务，处理完即离开；只有需要改变工作方式时才呼出 AI。采用浅色界面，冷调中性色和少量蓝色强调。

## 技术与样式

Tailwind CSS 4.3.3 使用官方 Vite 插件和 CSS-first `@theme`。Base UI 1.8.0 提供 Button、Dialog、Menu 的交互与无障碍行为，Lucide 提供统一线性图标。颜色与字体集中在 `src/web/style.css`，布局使用 Tailwind utilities；仅少数共享控件组合为组件样式。不引入第二套主题或通用 UI DSL。

## 布局与操作

桌面左侧浅灰导航和搜索，主区域是任务列表，底部常驻添加行。手机将任务筛选与搜索移到顶部。任务以行呈现，用圆形状态按钮完成或重新打开，点击正文打开备注侧栏，删除收进详情。没有营销标题、日常鼓励语或示例填充。

AI 通过小型常驻按钮呼出侧栏；手机侧栏占满屏幕，桌面允许继续操作任务。无自动弹窗。确认界面依次显示需求理解、处理方式、具体改动、最终效果和数据影响；执行界面显示服务端步骤，禁止假百分比或计时推进。详细记录折叠，工作区与版本位于更多菜单。

## 细节

使用 Segoe UI / PingFang SC / Microsoft YaHei 系统字体。正文 14px，标题 26px；手机输入框 16px，主要触控操作至少 44px。仅主动作与选中状态使用蓝色。短时颜色过渡，尊重 reduced-motion。Base UI 管理 Escape、模态焦点和返回焦点，错误信息贴近当前操作。
