# Product

## Register

product

## Users

本地单所有者，记录和完成日常待办。需要改变软件行为时，通过常驻 AI 助手提出需求，理解方案并确认后执行。

## Product Purpose

让同一个应用、同一批任务持续获得新能力。日常任务是主流程，软件改造是按需使用的辅助能力。基础 Todo 不依赖模型；AI 未连接时如实显示，不伪造方案或生成进度。

## Brand Personality

干净、克制、熟悉。参考 Microsoft To Do，以清晰的任务列表和标准操作表达用法，不用助手插画、口号或说明性卡片抢占注意力。

## Design Principles

任务优先。默认只显示任务操作。AI 是常驻按钮和按需侧栏，不是页面导航项；版本和执行详情收进二级入口。自然语言先由服务端路由为任务操作、新建插件或修改现有插件；不明确则追问。结构化方案说明改动和最终效果，确认后执行。等待界面反映实际阶段，收起后任务继续，完成时轻量提示。

## Anti-references

不做工程控制台、泛用聊天首页、演示插件商城、装饰性卡片网格。不要多余的欢迎语、鼓励语、技术标签、示例数据按钮和自动出现的 AI 弹层。

## Accessibility & Inclusion

系统中文字体，明确标签、键盘焦点、输入法支持、触控操作区和 320px 重排。详情关闭后保留草稿并恢复焦点；响应丢失后重试不重复创建。保留历史任务和字段，不以界面清理删除数据。
