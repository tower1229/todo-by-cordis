# Product

## Register

product

## Users

本地单所有者，记录和完成日常待办。需要改变软件行为时，通过常驻“改进应用”入口提出需求；Agent 调查并澄清后展示计划，用户开始执行，体验已验证候选后再确认应用。

## Product Purpose

最高目标是得到架构优雅的自迭代软件模板。当前仓库就是模板，Todo 只是示例业务。后续任务必须直接服务于生成、验证、运行、继续修改或恢复，不为无关业务功能扩张范围。让同一个应用、同一批数据持续获得新能力。日常任务是主流程，软件改造是按需使用的辅助能力。基础 Todo 不依赖模型；AI 未连接时如实显示，不伪造方案或生成进度。

## Brand Personality

干净、克制、熟悉。参考 Microsoft To Do，以清晰的任务列表和标准操作表达用法，不用助手插画、口号或说明性卡片抢占注意力。

## Design Principles

任务优先。默认只显示任务操作。AI 是常驻按钮和按需侧栏，只负责应用自迭代，不执行普通任务操作或通用问答。Plan 阶段自行调查实现、依赖和能力缺口，只向用户澄清必要的业务取舍；展示目标、验收、步骤及数据影响后，由用户开始执行。

执行期间锁定需求，进入专门的进度视图，展示真实阶段、结果与修正尝试；可以离开继续使用 Todo，回来或刷新后恢复进度。范围内自动生成、验证和修正，候选体验后单独确认应用。Agent 可以修改业务实现与必要接口，但不能修改自身执行策略、验证与发布等保护机制。版本和详细日志保留在二级入口。当前设计见 [Agent 重构详细设计](docs/engineering/06-agent-redesign.md)。

## Anti-references

不做工程控制台、泛用聊天首页、演示插件商城、装饰性卡片网格。不要多余的欢迎语、鼓励语、技术标签、示例数据按钮和自动出现的 AI 弹层。

## Accessibility & Inclusion

系统中文字体，明确标签、键盘焦点、输入法支持、触控操作区和 320px 重排。详情关闭后保留草稿并恢复焦点；响应丢失后重试不重复创建。保留历史任务和字段，不以界面清理删除数据。
